# Phase 2 — Install core dependencies

Install the JavaScript state and UI libraries: npm install @react-navigation/native @react-navigation/bottom-tabs react-native-safe-area-context react-native-screens zustand lucide-react-native react-native-reanimated @react-native-async-storage/async-storage. These are the same for Expo Go testing.
