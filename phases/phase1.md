# Phase 1 — Create the Expo project

Run npx create-expo-app voice-rag-assistant-mobile --template blank --typescript in your Ai Projects folder (or use ./ if already in the folder). This scaffolds the base Expo project with TypeScript support.
