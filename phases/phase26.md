# Phase 26 — Create a development build with EAS

Create a development build: eas build --profile development --platform android (or ios if on Mac). Install the built APK/IPA on a physical device. Verify the app runs with the dev client — this is the checkpoint that native linking works before writing any voice code.
