# Phase 29 — Add voice transcripts to the conversation history

Listen to VAPI's message events for transcript data and append the user's spoken questions and the assistant's spoken answers to the conversationStore, so voice and text interactions appear together in the conversation history. Persist to AsyncStorage alongside text messages.
