# Phase 34 — Build and test the APK (or IPA)

Create the final Android APK: eas build --profile production --platform android (or ios for Apple devices). Install the APK on a device and verify the full app works from the installed build (not the dev client): upload, indexing, text query, voice query, document deletion, dark/light toggle, all with no errors.
