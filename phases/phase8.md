# Phase 8 — Build the theme provider

Create src/theme/ThemeProvider.tsx: a React context that exposes the current palette, mode (light/dark), and a toggle function. It should respect the phone's system color scheme on first load (useColorScheme) and allow manual override.
