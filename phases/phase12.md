# Phase 12 — Build the document card component

Create src/components/DocumentCard.tsx matching the mockup: file-text icon (moss when ready, ochre when indexing), file name (single line, ellipsized), a delete icon button, and a status badge — 'Ready · N chunks' (moss tint), 'Indexing…' (ochre tint), or the error message (alert tint) for failed documents. Use theme tokens for all colors.
