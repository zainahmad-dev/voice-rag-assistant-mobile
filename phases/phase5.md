# Phase 5 — Configure environment variables

Create a .env file with EXPO_PUBLIC_API_URL pointing to the deployed backend (https://voice-rag-assistant-blush.vercel.app). Create .env.example for documentation. Explain how EXPO_PUBLIC_ prefix exposes variables to the app.
