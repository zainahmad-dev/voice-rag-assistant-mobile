# Phase 16 — Create upload helper functions

Create src/lib/upload.ts with helper functions: validateFile (check file size ≤15MB, type matches), and formatFormData (prepare the multipart FormData correctly for React Native). Keep upload logic separate for testability.
