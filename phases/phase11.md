# Phase 11 — Build the upload button component

Create src/components/UploadButton.tsx: a dashed-border tappable zone (matching the mockup) that opens expo-document-picker restricted to PDF/DOCX/TXT mime types, shows an uploading spinner state, and calls an onPick callback with the selected file's uri, name, and mimeType. Use the theme colors for borders and icons.
