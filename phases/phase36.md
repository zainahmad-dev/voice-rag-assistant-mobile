# Phase 36 — Run final testing and package for submission

Run a final end-to-end checklist on the installed APK: upload each supported file type (PDF, DOCX, TXT), watch indexing complete, ask an answerable voice question, ask an unanswerable one (expect grounded 'the documents don't cover that' response), delete a document, toggle dark/light mode, test with airplane mode on (confirm error handling). Then package the submission: repository link, installable APK link, screenshots, and a short demo note. Mark it done.
