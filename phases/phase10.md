# Phase 10 — Build the Library screen skeleton

Create src/screens/LibraryScreen.tsx with the header: app title 'Voice RAG Assistant' in the display font, subtitle showing 'N documents indexed', and a theme toggle button (moon/sun icon from lucide-react-native) that calls ThemeProvider's toggle. Body stays empty for now — just the header structure.
