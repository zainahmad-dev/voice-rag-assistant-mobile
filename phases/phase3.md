# Phase 3 — Install Expo packages

Install the Expo-specific packages: npx expo install expo-document-picker expo-status-bar expo-font react-native-svg. Also install the Google fonts: npx expo install @expo-google-fonts/space-grotesk @expo-google-fonts/inter @expo-google-fonts/jetbrains-mono (matches the web app design system).
