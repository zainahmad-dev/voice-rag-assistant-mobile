# Phase 35 — Write the project README

Write voice-rag-assistant-mobile/README.md: overview, how it relates to the existing web app and shared backend, tech stack decisions and reasoning (Expo dev-client for native VAPI support, bottom tabs for mobile navigation, same design tokens as web), environment variables needed, local development steps (npm install, npx expo start, then eas build for native), and EAS build instructions. Include the deployment deadline (22 July 2026) and submission requirements.
