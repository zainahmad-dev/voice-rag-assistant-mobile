# Phase 13 — Build the empty state component

Create src/components/EmptyState.tsx: a friendly message 'No documents yet — upload one to get started.' in the ink-soft color, used when the document list is empty.
