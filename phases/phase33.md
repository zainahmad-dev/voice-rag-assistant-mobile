# Phase 33 — Configure EAS build profiles for submission

Update eas.json with preview and production profiles. Preview is for testing APK, production is the final submission build. Both profiles should include environment variables (EXPO_PUBLIC_API_URL, EXPO_PUBLIC_VAPI_PUBLIC_KEY). Document which profile is which.
