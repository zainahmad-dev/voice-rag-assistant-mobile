# Phase 19 — Build the message bubble component

Create src/components/MessageBubble.tsx matching the mockup: user messages as right-aligned moss-filled bubbles with light text (bubble-text color), assistant messages as left-aligned surface cards with a hairline border (line color) and an optional monospace source line (e.g. 'similarity 0.87') underneath.
