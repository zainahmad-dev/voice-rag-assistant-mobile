# Phase 30 — Add error handling and loading states

Review every flow and add user-friendly error handling: unsupported file type, file too large, network failures on upload/list/delete/query, mic permission denied, voice call failures. Each message should say what happened and what to do next. Create src/components/Loading.tsx with spinner and label. Use the Loading component in all async operations.
