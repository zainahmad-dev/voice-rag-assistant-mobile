# Phase 14 — Build the document list

Build the Library screen body: a FlatList of DocumentCard items with the UploadButton as the list header, the EmptyState component when there are no documents, pull-to-refresh (SafeAreaView context), and item separators. Use mock data for now — no API calls yet.
