# Phase 18 — Build the Assistant screen skeleton

Create src/screens/AssistantScreen.tsx layout: a FlatList conversation area that fills the screen, the voice orb centered below it, and a text input bar pinned at the bottom, wrapped in KeyboardAvoidingView so the keyboard doesn't cover the input.
