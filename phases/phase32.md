# Phase 32 — Cross-client verification

Verify both clients share one backend: upload a document from the web app and confirm it appears in the mobile library (document list), upload from mobile and confirm it appears on the web, and confirm a voice question on mobile retrieves content from a document uploaded on the web. This proves single-backend architecture works.
