# Phase 9 — Set up bottom tab navigation

Create src/navigation/BottomTabs.tsx: NavigationContainer and createBottomTabNavigator with two tabs — Library (books icon from lucide-react-native) and Assistant (mic icon). Style the tab bar with theme colors (surface background, moss active tint, ink-soft inactive). Use placeholder screens for now.
