# Phase 23 — Set up expo-dev-client and EAS

Install expo-dev-client: npm install expo-dev-client. Install the EAS CLI: npm install -g eas-cli. Log in to your EAS account: eas login. Run eas build:configure to set up the project. Explain why Expo Go doesn't support native modules like VAPI, and why dev-client is needed.
